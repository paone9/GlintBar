# Direct dependencies (human-edited source).
# Compile the pinned, hash-locked requirements.txt with:
#   pip install pip-tools
#   pip-compile --generate-hashes --output-file=requirements.txt requirements.in
pywebview==6.2.1
psutil==7.2.2
